# $${\color{palegreen} PaleGreen }$$
### A tiny client-side js library for building user interfaces.

Maybe it's a bit of exaduration to call this as a library. It is rather a little wrapper logic that allows to write the UI code in a certain structure, but still really close to vanilla js style.

// TODO : counter example here

#### Examples
Please take a look at the examples directory of this repo. You can try them out following the repo checkout using a local web server.
